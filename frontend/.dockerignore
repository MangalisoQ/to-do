# Ignore node_modules — you'll install fresh ones in the container
node_modules
npm-debug.log
yarn-debug.log
yarn-error.log
pnpm-debug.log

# Environment files
.env
.env.local
.env.*.local

# OS + editor junk
.DS_Store
Thumbs.db

# Git files
.git
.gitignore

# Docker files (optional if not needed inside the image)
.dockerignore
docker-compose.yml
Dockerfile*

# Tests & coverage
coverage
*.test.js
*.spec.js
jest.config.js

# Optional: build artifacts if you're compiling (e.g., TypeScript)
dist
build
*.md
